import React from 'react';
import {
   View,
   TouchableHighlight,
   TouchableOpacity,
   TouchableWithoutFeedback
} from 'react-native'

function PlayPause(props){
   return(
       <View>

       </View>
   )
}

export default PlayPause